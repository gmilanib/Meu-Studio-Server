# Banco exclusivo de testes. Não reutiliza PGHOST/PGDATABASE da aplicação.
spring.datasource.url=${TEST_DATABASE_URL:jdbc:postgresql://127.0.0.1:55439/meu_studio_procedimentos_test}
spring.datasource.username=${TEST_DATABASE_USER:gmb}
spring.datasource.password=${TEST_DATABASE_PASSWORD:}
app.bootstrap-admin.enabled=false
